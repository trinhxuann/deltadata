% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downloadFiles.R
\name{getCNRA}
\alias{getCNRA}
\title{Pull files from a CNRA data package}
\usage{
getCNRA(dataset, files, quiet = FALSE)
}
\arguments{
\item{dataset}{URL or slug of the CNRA dataset (e.g., "dayflow" or
"https://data.cnra.ca.gov/dataset/dayflow").}

\item{files}{Vector of file names, resource IDs, or resource names to download.
Leave blank to see available options.}

\item{quiet}{Defaults to FALSE. If TRUE, will not print the summary table when \code{files} is omitted.}
}
\value{
If \code{files} is omitted, invisibly returns a data frame of available resources.
If \code{files} is provided, returns a named list of data frames (for CSVs/RDS) or local file paths.
}
\description{
Interacts with the data.cnra.ca.gov API to query and download dataset resources.
If no files are specified, returns an opinionated summary table of available
files. CSV files are read directly into R data frames, while other file types
are downloaded to temporary storage and their file paths returned.
}
\examples{
\dontrun{
# Inspect available files in the Dayflow dataset
getCNRA("https://data.cnra.ca.gov/dataset/dayflow")

# Download specific resource(s)
dayflowData <- getCNRA("dayflow", files = "Dayflow Results 1929 - 2022")
}
}
